<!DOCTYPE html>
<html lang="en">
  <head> </head>
  <body>
    <section id="features" class="ud-features">
      <h1>Hello world</h1>
      <% products.map((products)=>{%>
      <h4>
        <a href="/products/<%= products.id %>"><%= products.productTitle %></a>
      </h4>
      <p><%= products.productDescription %></p>
      <p>ราคา : <%= products.productPrice %> บาท</p>
      <%})%>
    </section>
  </body>
</html>
